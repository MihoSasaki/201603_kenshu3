<%= form_for(@board) do |f| %>
  <% if @board.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@board.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @board.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="row">
  <div class="col-md-offset-3">
    <%= f.label :title %>
    <%= f.text_area :title, placeholder: "Compose new title..." %><br>

  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
  </div>
</div>